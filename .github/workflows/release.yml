name: release
on:
  push:
    tags:
      - 'v*'
jobs:
  build-deb:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        dist:
          - buster
          - bullseye
        arch:
          - i386
          - amd64
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: 'true'
          repository: ennorehling/csmapfx
      - name: Build Debian Package
        uses: ennorehling/action-debian-package@c74ec7fd4f1e9692833a9a8dcd203953ec9d8abe
        with:
           os_distribution: bullseye
           cpu_architecture: ${{ matrix.arch }}
           artifacts_directory: artifacts
      - name: Upload to Packagecloud
        uses: danielmundi/upload-packagecloud@v1
        with:
          package-name: artifacts/csmapfx_*.deb
          packagecloud-username: enno
          packagecloud-repo: eressea
          packagecloud-distrib: debian/${{ matrix.dist }}
          packagecloud-token: ${{ secrets.PACKAGECLOUD_TOKEN }}
